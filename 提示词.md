# WQB Alpha 自进化 Agent Prompt

从零搭建一个面向 **WorldQuant BRAIN** 的小规模 Alpha 自进化研究 Agent。

目标闭环：

```text
Memory → Hypothesis → Field Discovery → Candidate
→ Real Simulation → Reflection → Memory → Next Experiment
```

## 可用数据集

```text
Analyst:       analyst4
Fundamental:   fundamental2, fundamental6
Model:         model16, model51
News:          news12, news18
Option:        option8, option9
Price Volume:  pv1, pv13, univ1
Social Media:  socialmedia12, socialmedia8
```

优先级可参考 Value Score，但不得机械按分数搜索：

```text
Model 7 > Option 6 > Analyst 5 > Fundamental/News 3 > Price Volume/Social 2
```

## Field Discovery

字段**不需要永久保存到本地**。

Agent 根据当前 Research Question / Hypothesis 自主选择 Dataset，并调用 WQB API 获取真实 Fields。

流程：

```text
Hypothesis
→ 选择 Dataset
→ API 分页拉取 Fields
→ 当前内存中检索 description / type / metadata
→ 选择少量相关 Fields
→ 构造 Candidate
→ Simulation
```

要求：
* 在同一个进程中可以最多同时模拟三次，轮询模拟节省时间。
* 禁止虚构 Field。
* 只使用 API 实际返回的 Field。
* 不要一次拉取全部字段。
* 使用 API 的 `limit / offset` 或实际分页机制。
* 每次请求使用较小固定 LIMIT；优先搜索，再按需继续分页。
* 达到当前研究所需字段数量后立即停止拉取。
* 不为“可能以后有用”而遍历全部 Dataset。
* Field metadata 可临时缓存于当前运行，但不要求持久化。
* Memory 只记录 Dataset/Field 的研究结论，不保存完整 Field Catalog。

## 每轮研究

1. 读取 current_best、lessons、avoid、next。
2. 形成一个明确 Hypothesis。
3. 按 Hypothesis 选择 Dataset，并通过 API 检索 Fields。
4. 提出 **5~6个 Candidate**，优先单变量局部修改。
5. 真实进行 BRAIN Simulation
6. 读取 Sharpe、Fitness、Turnover、Margin、Checks。
7. FAIL 先诊断，再针对性修改。
8. 更新 Current Best 和压缩 Memory。

## 约束

* 只使用真实 WQB Fields、Operators、Syntax、Settings。
* 不得编造 Simulation 结果。
* 不随机堆 Operator。
* 不暴力执行 `所有 Fields × Operators × Windows`。
* 不反复微调特殊窗口或参数。
* 已明确失败且无新理由的方向不要重复。
* 单次成功不能升级为稳定经验。
* API 调用和 Simulation 都视为研究成本，应按需使用。
* 上下文保持最小，只读取与当前实验最相关的历史。

## MVP

只实现：

```text
ResearchState
Experiment
Trajectory
Reflection
Experience Memory
Current Best
Field API Discovery
Notebook Simulation
```

不要优先建设多 Agent、Knowledge Graph、Vector DB、复杂 Scheduler 或 Web UI。

核心原则：

> **按假设找字段，而不是按字段找公式。**

> **真实 Simulation 产生证据，证据形成经验，经验改变下一轮实验。**